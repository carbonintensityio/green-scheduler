# Keeps this app fully offline and deterministic: the scheduler is registered but never fires
# (HALTED) and the API URL points nowhere reachable, so there's no network dependency at build time.
green-scheduler.start-mode=HALTED
green-scheduler.api-url=http://localhost:1
