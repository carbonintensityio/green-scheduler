<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <artifactId>green-scheduler-build-parent</artifactId>
        <groupId>io.carbonintensity</groupId>
        <version>${revision}</version>
        <relativePath>../build-parent/pom.xml</relativePath>
    </parent>
    <artifactId>scheduler-coverage-report</artifactId>
    <name>Scheduler Test Coverage Report</name>
    <description>Aggregates the jacoco.exec files of core and execution-planner (built with the
        test-coverage profile) into a single multi-module HTML/XML report, using jacoco's own
        report-aggregate goal instead of a hand-rolled shell script.
    </description>
    <packaging>pom</packaging>

    <properties>
        <format.skip>true</format.skip>
        <revision>999-SNAPSHOT</revision>
    </properties>

    <dependencies>
        <!-- report-aggregate reads each dependency's target/classes and target/jacoco.exec directly
             from the reactor - no shell script, no manually-maintained module list to go stale. -->
        <dependency>
            <groupId>io.carbonintensity</groupId>
            <artifactId>green-scheduler-core</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>io.carbonintensity</groupId>
            <artifactId>execution-planner</artifactId>
            <version>${project.version}</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>${jacoco.version}</version>
                <executions>
                    <execution>
                        <id>report-aggregate</id>
                        <phase>prepare-package</phase>
                        <goals>
                            <goal>report-aggregate</goal>
                        </goals>
                        <configuration>
                            <title>Scheduler</title>
                            <footer>Code Coverage Report for scheduler ${project.version}</footer>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
