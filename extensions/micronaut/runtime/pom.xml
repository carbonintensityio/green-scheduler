<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>io.carbonintensity</groupId>
        <artifactId>green-scheduler-micronaut-parent</artifactId>
        <version>${revision}</version>
        <relativePath>../pom.xml</relativePath>
    </parent>
    <artifactId>green-scheduler-micronaut</artifactId>
    <name>Green Scheduler - Micronaut Extension - Runtime</name>
    <description>Runtime support for scheduling @GreenScheduled methods in Micronaut applications</description>

    <dependencies>
        <dependency>
            <groupId>io.carbonintensity</groupId>
            <artifactId>green-scheduler-core</artifactId>
        </dependency>
        <dependency>
            <groupId>io.micronaut</groupId>
            <artifactId>micronaut-context</artifactId>
        </dependency>

        <!--
            Build time only companion module. Declared with provided scope so that the reactor
            builds it before this module (it is used below as an annotation processor path).
            It is not required on the runtime classpath of applications.
        -->
        <dependency>
            <groupId>io.carbonintensity</groupId>
            <artifactId>green-scheduler-micronaut-processor</artifactId>
            <version>${revision}</version>
            <scope>provided</scope>
        </dependency>

        <!-- Test dependencies -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.awaitility</groupId>
            <artifactId>awaitility</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-simple</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>io.micronaut</groupId>
                            <artifactId>micronaut-inject-java</artifactId>
                            <version>${micronaut.version}</version>
                        </path>
                        <!-- Build time processing of @GreenScheduled (used by the integration tests) -->
                        <path>
                            <groupId>io.carbonintensity</groupId>
                            <artifactId>green-scheduler-micronaut-processor</artifactId>
                            <version>${revision}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
