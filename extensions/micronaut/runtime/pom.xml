<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>io.carbonintensity</groupId>
        <artifactId>green-scheduler-micronaut-parent</artifactId>
        <version>${revision}</version>
        <relativePath>../pom.xml</relativePath>
    </parent>
    <artifactId>green-scheduler-micronaut</artifactId>
    <name>Green Scheduler - Micronaut Extension - Runtime</name>
    <description>Runtime support for scheduling @GreenScheduled methods in Micronaut applications</description>

    <dependencies>
        <dependency>
            <groupId>io.carbonintensity</groupId>
            <artifactId>green-scheduler-core</artifactId>
        </dependency>
        <dependency>
            <groupId>io.micronaut</groupId>
            <artifactId>micronaut-context</artifactId>
        </dependency>

        <!--
            Build time only companion module. Declared with provided scope so that the reactor
            builds it before this module (it is used below as an annotation processor path).
            It is not required on the runtime classpath of applications.
        -->
        <dependency>
            <groupId>io.carbonintensity</groupId>
            <artifactId>green-scheduler-micronaut-processor</artifactId>
            <version>${revision}</version>
            <scope>provided</scope>
        </dependency>

        <!--
            Micrometer meter registration (see GreenSchedulerMetricsBinder): optional so that consumers who never
            add @GreenObserved, or who don't have Micrometer on their classpath, don't pull it in.
            Unlike the Quarkus extension's own micrometer-core dependency, this one cannot be left unversioned:
            micronaut-core-bom (imported above) only manages micronaut-core's own modules, not the separate
            micronaut-micrometer/Micrometer ecosystem, so there is nothing here for Maven to resolve a version
            from. ${micrometer.version} exists purely so *this reactor* can compile against the Micrometer API -
            being optional, it is never forced onto a consumer's classpath or version resolution; an application
            that actually wants these metrics adds its own Micrometer stack (typically micronaut-micrometer) and
            that version wins for them, same as if this dependency were absent entirely.
        -->
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-core</artifactId>
            <version>${micrometer.version}</version>
            <optional>true</optional>
        </dependency>

        <!-- Test dependencies -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.awaitility</groupId>
            <artifactId>awaitility</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-simple</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>io.micronaut</groupId>
                            <artifactId>micronaut-inject-java</artifactId>
                            <version>${micronaut.version}</version>
                        </path>
                        <!-- Build time processing of @GreenScheduled (used by the integration tests) -->
                        <path>
                            <groupId>io.carbonintensity</groupId>
                            <artifactId>green-scheduler-micronaut-processor</artifactId>
                            <version>${revision}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
